//Simple C Program to Find ASCII Value
#include <stdio.h>
#include <stdio.h>
int main() {
    char ch;
    clrscr();
    printf("Enter a character: ");
    scanf("%c", &ch);
    printf("ASCII value of '%c' is %d\n", ch, ch);
    getch();
    return 0;
}
